#1:install virtualenv for the virtual environment
$pip install virtualenv
$cd project_dir
$virtualenv venv
$virtualenv -p /usr/bin/python3.x venv
$source venv/bin/activate #for actiavting the environment
$deactivate #to deactivate the environment

#2 install pyowm library for weather API-*need to register with OpenWeatherMap to get the weather entries
$pip install pyowm**** last letter is ***m***
